import 'package:flutter/material.dart';

class monitor_screen extends StatelessWidget {
  const monitor_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("monitor_screen"));
  }
}
